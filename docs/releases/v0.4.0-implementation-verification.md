# Rocker v0.4.0 Implementation Verification

Date: 2026-09-04 (UTC)
Status: implementation complete in the development line; not released

This record verifies the v0.4 Terminal Productivity implementation. The
product milestone is named `v0.4.0`, while the package version remains `0.3.3`
until a release is explicitly requested. No tag, GitHub release, or installer
was created for this implementation pass.

## Commit Identity

| Point | Commit |
| --- | --- |
| Base before v0.4 implementation | `6781408c1a549e82dfaf01b454eba6d20a315a25` |
| Task 5 implementation commit | `3edc05b20824668c793e5b2541fdf7cd4a141f4f` |
| Task 5 final commit verified here | `4c8ae51e5b8c730431880feac6676ba70e724e07` |
| Task 6 verification record | this document's commit; the final hash is reported in the handoff |

The Task 5 report identifies `3edc05b` as its implementation commit. The
follow-up `4c8ae51` is the final Task 5 tree used as the parent for this
verification pass.

## Scope Verified

- Per-Session terminal search remains renderer-local and uses the xterm SearchAddon.
- Command Palette, terminal context actions, session menus, and visible actions use the shared command surface.
- Shell key combinations remain available to xterm and the remote shell.
- Session rename, duplicate, split, close, and recent ordering preserve the existing owner and connection contracts.
- Live terminal appearance changes apply without reconnecting or clearing a Session.
- Blocked Settings persistence leaves temporary appearance changes active without claiming they were saved.
- SFTP, Snippets, and Local Terminal remain reachable placeholders.
- Production dependencies remain limited to `ssh2`; `@xterm/addon-search` remains development-only.

## Verification Commands and Results

The release-surface test was written before the documentation files. Its red
run established that the new evidence contract failed only because these files
were absent:

```text
npm test -- tests/packaging.test.ts
Test Files  1 failed (1)
Tests       1 failed | 7 passed (8)
Failure     v0.4 implementation evidence and smoke checklist were absent
```

The final commands below were run after the documentation and status updates
were added.

| Check | Command | Result | Evidence |
| --- | --- | --- | --- |
| Focused packaging and i18n tests | `npm test -- tests/packaging.test.ts tests/i18n.test.ts` | PASS | 2 files passed; 10 tests passed. |
| Full test suite | `npm test` | PASS | 62 files passed, 1 skipped; 528 tests passed, 1 skipped. |
| TypeScript validation | `npm run typecheck` | PASS | `tsc --noEmit` completed successfully. |
| Renderer/main build | `npm run build` | PASS | electron-vite built main, preload, and renderer bundles. |
| Working-tree whitespace | `git diff --check` | PASS | Completed without whitespace errors. |
| Short local soak smoke signal | `npm run test:soak -- --duration-ms=3000` | PASS | Short v0 smoke signal only; it is not the v1 long-soak gate. |
| Production dependency audit | `timeout 90s env npm_config_registry=https://registry.npmjs.org npm audit --omit=dev --audit-level=moderate` | TIMEOUT | Official npm registry did not return before the 90-second bound; no vulnerability conclusion is claimed. |

The full-suite count above is from the final local tree. The official registry
audit timed out at the bounded limit, so this record does not claim an audit
pass. The GitHub workflow treats an unavailable audit endpoint as a verification
failure and must be rerun when the endpoint is available.

## Environment Limits

- Verification host: Linux development environment. The repository requires
  native Windows or macOS for native packaged application startup coverage.
- `npm run build` verifies Electron bundles only; no installer was produced.
- The native Windows/macOS startup checks are `DEFERRED` for the v0
  implementation line, along with native install, reconnect, close, uninstall,
  and packaged-artifact checks.
- The 30-minute long soak is a v1 release gate and is `DEFERRED` for v0.4.
  The bounded local soak above is only a smoke signal.
- Windows/Wine packaging and native macOS packaging were not used to claim
  release artifacts in this implementation record.

## Release Boundary

The workflow continues to build only Windows and macOS installer/archive
targets. Its long-soak job remains gated to `v1.*` tags or an explicit manual
workflow dispatch, with a 45-minute job timeout. v0.4 implementation completion
does not change the package version, create a tag, publish a release, or imply
that an installer has been validated on a native platform.

The manual workflow is [the v0.4 smoke checklist](./v0.4.0-smoke-checklist.md).
