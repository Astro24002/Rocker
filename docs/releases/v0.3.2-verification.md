# Rocker v0.3.2 Verification Record

This record is intentionally evidence-based. `PENDING` means the command has
not been run for the release-candidate commit; it is not a pass.

## Local Automated Matrix

| Check | Command | Result | Evidence summary |
| --- | --- | --- | --- |
| Unit and integration tests | `npm test` | PENDING |  |
| TypeScript validation | `npm run typecheck` | PENDING |  |
| Renderer/main production build | `npm run build` | PENDING |  |
| Production dependency audit | `env npm_config_registry=https://registry.npmjs.org npm audit --omit=dev --audit-level=moderate` | PENDING |  |
| Deterministic SSH soak | `npm run test:soak` | PENDING | Requires 30-minute run; exact resource baseline and RSS result. |
| Working-tree whitespace | `git diff --check` | PENDING |  |

## Container Packaging Checks

| Check | Command | Result | Evidence summary |
| --- | --- | --- | --- |
| Windows/Wine package image | `docker build -f containers/windows-builder.Dockerfile .` | PENDING |  |
| Windows artifact package smoke | `scripts/run-windows-container-smoke.sh` | PENDING |  |
| macOS native runner package and launch | `scripts/run-macos-smoke.sh` | PENDING | Must run on macOS; a Linux container is not equivalent. |

## CI Gate

- The `soak` job runs only for a `v*` tag or explicit `workflow_dispatch` and
  has a 45-minute timeout.
- The release job requires both `package` and `soak`.
- Release publication is limited to the six allow-listed versioned installer
  and archive assets.

## Release Identity

| Field | Value |
| --- | --- |
| Version | `0.3.2` |
| Candidate commit | pending |
| Tag | pending; do not create until evidence is complete |
| GitHub release | pending; do not publish until evidence is complete |

## Platform Smoke Evidence

Complete [the platform checklist](./v0.3.2-smoke-checklist.md) against the same
candidate commit before changing any status here to complete.
